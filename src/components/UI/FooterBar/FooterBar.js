import React from 'react';
import classes from './FooterBar.module.css';

const FooterBar = (props) => {
    return (
        <div className={classes.FooterBar}>
            design and code by jay fry, powered by react
        </div>
    )
}

export default FooterBar;